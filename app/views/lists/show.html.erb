<h1>See the movies in <%= @list.name %></h1>


<ul class="list-group">
  <% @list.movies.each do |movie| %>
    <li class="list-group-item"><%= movie.title %></li>
  <% end %>
</ul>


<%= link_to "Back to Lists", lists_path %>

<%= link_to "Add to the list", new_list_bookmark_path(@list) %>
